{
  "$schema": "./config.schema.json",
  "version": "1.0",
  "description": "LLM Provider Configuration Template - Copy to config.json and customize",

  "provider": {
    "name": "anthropic",
    "description": "Select your LLM provider: anthropic, openai, azure, google, ollama, custom",
    "options": ["anthropic", "openai", "azure", "google", "ollama", "custom"]
  },

  "model": {
    "name": "claude-sonnet-4-20250514",
    "description": "Model to use for the selected provider",
    "examples": {
      "anthropic": ["claude-sonnet-4-20250514", "claude-opus-4-20250514", "claude-3-5-sonnet-20241022"],
      "openai": ["gpt-4o", "gpt-4-turbo", "gpt-4", "gpt-3.5-turbo"],
      "azure": ["gpt-4", "gpt-4-turbo", "gpt-35-turbo"],
      "google": ["gemini-pro", "gemini-ultra", "gemini-1.5-pro"],
      "ollama": ["llama3", "llama2", "mistral", "codellama"],
      "custom": ["your-model-name"]
    }
  },

  "api": {
    "key_env_var": "LLM_API_KEY",
    "description": "Environment variable name containing the API key",
    "base_url": null,
    "base_url_description": "Optional: Custom API endpoint (required for Azure, Ollama, custom providers)"
  },

  "settings": {
    "temperature": 0.7,
    "max_tokens": 4096,
    "timeout_seconds": 120
  },

  "agent_tool": {
    "name": "claude-code",
    "description": "CLI tool used for agent interactions",
    "options": ["claude-code", "aider", "cursor", "continue", "custom"],
    "config_path": null
  }
}
