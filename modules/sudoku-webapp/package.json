{
  "name": "sudoku-webapp",
  "version": "1.0.0",
  "description": "Sudoku Web Application with React frontend and Node.js backend",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run lint",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "clean": "rm -rf frontend/dist frontend/node_modules backend/node_modules .env.local",
    "db:init": "bash database/init.sh",
    "db:migrate": "psql -U postgres -d sudoku_game -f database/migrations/001-initial-schema.sql"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
